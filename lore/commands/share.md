---
description: Share the current Claude Code session to Lore and get a shareable URL.
---

Invoke the `share` skill to export this session to Lore. Pass any extra instruction the user gave (e.g. "publicly", "private") through to the skill so it can pick the right `--visibility`.
