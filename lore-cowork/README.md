# lore-cowork (deprecated)

This plugin has been renamed and merged with `lore`. Please run `/plugin uninstall lore-cowork && /plugin install lore`. The new `lore` plugin includes the same MCP server and the same slash commands (`/share`, `/lore`) with no behavior change.
